<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/header.css"> <!-- Use absolute path for the stylesheet -->
    <link rel="stylesheet" href="/css/footer.css"> <!-- Use absolute path for the stylesheet -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" type="text/css"href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <link rel="shortcut icon" href="/images/favicon.png" type="image/x-icon">
    <title>Search Results</title>
    <link rel="stylesheet" href="/css/search.css">
</head>
<body>
    
    <%- include('partials/header.ejs') %>

    <main class="content">
        
        <div class="suggestion"> <!-- Container for suggestions -->
            <div class="image-container"> <!-- Flexbox container for images -->
                <% if (movies.length > 0) { %>
                    <% movies.forEach(movie => { %>
                        <div class="image-item"> <!-- Each movie image item -->
                            <a href="/movies/details/<%= movie.id %>"> <!-- Pass movie ID here -->
                                <img src="https://image.tmdb.org/t/p/w500<%= movie.poster_path %>" alt="<%= movie.title %> Poster">
                                <div class="overlay">
                                    <span class="plus-sign">More details</span>
                                </div>
                            </a>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p class="no-found">No related movies found for "<%= searchTerm %>".</p>
                <% } %>
            </div>
        </div>
    </main>

    <%- include('partials/footer.ejs') %>

</body>
<script src="/js/scripts.js"></script> <!-- Use absolute path for the script -->

</html>

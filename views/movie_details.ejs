<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Details - <%= movie.title %></title>
    <link rel="stylesheet" href="/css/movieDetails.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
    <link rel="shortcut icon" href="/images/favicon.png" type="image/x-icon">
</head>
<body>
    <!-- Header -->
    <header>
        <nav>
            <!-- Exit Arrow -->
            <div class="Home">
                <a href="/"><i class="fa fa-arrow-left"></i> HOME</a>
            </div>
        </nav>
    </header>
    
    <!-- Main Content -->
    <main class="content">
        <% if (movie) { %>
            <!-- Movie Poster -->
            <div class="movie-poster">
                <img src="<%= movie.posterPath %>" alt="<%= movie.title %> Poster" id="moviePoster">
            </div>
            
            <!-- Movie Details -->
            <div class="movie-details">
                <h1 id="movieTitle"><%= movie.title %></h1>
                <div class="rating">
                    <span><i class="fa fa-star"></i> <span id="movieRating"><%= movie.rating %></span> IMDb</span>
                    <span><i class="fa fa-calendar-alt"></i> <span id="movieYear"><%= new Date(movie.releaseDate).getFullYear() %></span></span>
                    <span><i class="fa fa-clock"></i> <span id="movieDuration"><%= movie.duration || 'N/A' %></span></span>
                    <span><i class="fa fa-user-alt"></i> <span id="movieAgeRating"><%= movie.ageRating || 'N/A' %></span></span>
                </div>
                <p class="description" id="movieDescription"><%= movie.overview %></p>
            </div>
        <% } else { %>
            <p>Movie not found!</p>
        <% } %>
    </main>
    
    <!-- Footer -->
    <footer>
        <p>&copy; Movie Recommendation System</p>
    </footer>
</body>
</html>
